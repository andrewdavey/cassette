dotless is copied from https://github.com/dotless/dotless

The core library has been modified to remove the ASP.NET dependencies.
Cassette only needs the compilation aspect of dotless.

The assembly has been renamed to Cassette.dotless.Core.dll to avoid conflicts with the existing library.