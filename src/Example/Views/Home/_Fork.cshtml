@{
    // This is an example of an external script reference. Knapsack will generate the <script>
    // tag for us. In this example, we've set the location to be "body" so it'll appear at the
    // end of the page, just before </body>. (See _Layout.cshtml for the rendering calls.)
    Html.ReferenceScript("http://gitforked.com/api/1.1/button.js", location: "body");
}
<p>Knapsack is free, open source, software: 
<a href="https://github.com/andrewdavey/knapsack" class="gitforked-button gitforked-forks gitforked-watchers">Fork</a>
</p>

